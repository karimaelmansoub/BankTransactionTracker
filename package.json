{
  "name": "uetr-tracker",
  "version": "1.0.0",
  "description": "Track UETR (Unique End-to-end Transaction Reference) payment statuses by polling the bank's payment-tracker API on a schedule.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev:server": "node --watch server/index.js",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client install && npm --prefix client run build",
    "install:all": "npm install && npm --prefix client install"
  },
  "dependencies": {
    "axios": "^1.17.0",
    "cheerio": "^1.2.0",
    "express": "^4.21.2",
    "node-cron": "^4.2.1"
  }
}
